/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

h1 {
  @apply text-3xl font-black text-center my-4 text-primary uppercase;
}

a {
  @apply btn btn-md btn-primary my-0.5 text-white;
}

div.nav {
  @apply flex flex-row justify-center items-center gap-1;
}
